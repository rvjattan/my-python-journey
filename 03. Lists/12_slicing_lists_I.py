# extracting only a portion of list is known as slicing
# example:

#extract letters from "b" to "e":

letters = ["a", "b", "c", "d", "e", "f", "g"]
sliced_letters = letters[1:5]
print(sliced_letters)